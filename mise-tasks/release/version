#!/usr/bin/env bash
#MISE description="Create a semantic-release version, changelog, tag, and GitHub release."
set -euo pipefail
source "$(cd "$(dirname "${BASH_SOURCE[0]}")/../.." && pwd)/scripts/release/lib.sh"
ensure_gh_token
npx semantic-release --no-ci --extends ./release.config.cjs
