# Locks

This directory contains committed tool and model identities used by a known workstation build. Model manifests are copied here only after a profile passes its smoke and context tests. Registry tags remain convenient inputs, but recovery should verify their current digests against these files.
